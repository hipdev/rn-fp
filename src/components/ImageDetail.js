import React from 'react'
import { View, Text, StyleSheet } from 'react-native'

const ImageDetail = () => {
  return <Text style={styles.textStyle}>Show image </Text>
}

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 30,
  },
})

export default ImageDetail
